name: Kiro PR Summary

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: kiro-summary-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  summarize:
    name: Generate PR Summary
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch base branch
        run: git fetch origin ${{ github.event.pull_request.base.ref }}

      - name: Collect PR metadata
        id: meta
        run: |
          FILES=$(git diff origin/${{ github.event.pull_request.base.ref }}...HEAD --name-only)
          STAT=$(git diff origin/${{ github.event.pull_request.base.ref }}...HEAD --stat)
          COMMITS=$(git log origin/${{ github.event.pull_request.base.ref }}..HEAD --oneline)

          {
            echo "## PR Metadata"
            echo ""
            echo "### Commits"
            echo '```'
            echo "$COMMITS"
            echo '```'
            echo ""
            echo "### Changed Files"
            echo '```'
            echo "$STAT"
            echo '```'
          } > /tmp/pr-meta.txt

      - name: Install Kiro CLI
        run: curl -fsSL https://cli.kiro.dev/install | bash

      - name: Generate PR summary
        env:
          KIRO_API_KEY: ${{ secrets.KIRO_API_KEY }}
          KIRO_LOG_NO_COLOR: "1"
        run: |
          META=$(cat /tmp/pr-meta.txt)

          kiro-cli chat \
            --no-interactive \
            --trust-all-tools \
            "Generate a concise, human-readable summary of this pull request. Read the changed files to understand what was done.

          ${META}

          Format the summary as:
          1. **What changed** — A 2-3 sentence overview
          2. **Key changes** — Bullet list of the important modifications
          3. **Impact** — What parts of the system are affected
          4. **Testing notes** — What should reviewers test manually

          Keep it brief and useful for reviewers who haven't seen the code yet." > /tmp/pr-raw.txt 2>&1 || true

          node .github/scripts/clean-kiro-output.js /tmp/pr-raw.txt /tmp/pr-summary.md

      - name: Update PR description
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const summary = fs.readFileSync('/tmp/pr-summary.md', 'utf8');

            const currentBody = context.payload.pull_request.body || '';
            const marker = '<!-- kiro-summary-start -->';
            const endMarker = '<!-- kiro-summary-end -->';

            const kiroSection = [
              marker,
              '## 📋 AI-Generated Summary',
              '',
              summary,
              '',
              `*Generated by [Kiro CLI](https://kiro.dev/cli/) • Updated at ${new Date().toISOString()}*`,
              endMarker,
            ].join('\n');

            let newBody;
            if (currentBody.includes(marker)) {
              const regex = new RegExp(`${marker}[\\s\\S]*?${endMarker}`);
              newBody = currentBody.replace(regex, kiroSection);
            } else {
              newBody = currentBody + '\n\n' + kiroSection;
            }

            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              body: newBody,
            });
